/**
 * Created by sindh on 21-09-2016.
 */

/*
 function getAuthorization(username, password) {
    return 'Basic ' + btoa(username+':'+password);
 }

 // Use like this:
 if username and password is correct:
    $http.defaults.headers.common['Authorization'] = getAuthorization('charan', 'charan');
 else
    tell the user that password or username is wrong
 */